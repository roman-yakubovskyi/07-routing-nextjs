import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Profile',
  description: 'Profile page of my app',
};

const Profile = () => {
  return <div>Profile</div>;
};
export default Profile;
